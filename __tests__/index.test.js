import { describe, it } from 'node:test';
import assert from 'node:assert/strict';

describe('Test someting new in Node.js 20', () => {
  it('should test 1 equal to 1', () => {
    assert.strictEqual(1, 1);
  });
  it('should test 2 equal to 2', () => {
    assert.strictEqual(1, 1);
  });
  it('should test 3 equal to 3', () => {
    assert.strictEqual(1, 1);
  });
});
